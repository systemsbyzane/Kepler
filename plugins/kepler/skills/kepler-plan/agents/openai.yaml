interface:
  display_name: "Kepler Plan"
  short_description: "Create right-sized multi-project plans"
  default_prompt: "Use $kepler-plan to turn this request into a right-sized, executable plan."
policy:
  allow_implicit_invocation: true
