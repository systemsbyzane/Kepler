interface:
  display_name: "Kepler Platform"
  short_description: "Coordinate platform and environment work"
  default_prompt: "Use $kepler-platform to coordinate this platform change across source and runtime owners."
policy:
  allow_implicit_invocation: true
