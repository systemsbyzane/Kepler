# Development workload

Application and service repositories live here. Each checkout owns its layout,
commands, tests, and applicable `AGENTS.md`; install a Kepler bridge and
dispatch implementation to the saved repository project.

