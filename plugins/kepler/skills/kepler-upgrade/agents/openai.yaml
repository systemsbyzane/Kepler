interface:
  display_name: "Kepler Upgrade"
  short_description: "Upgrade safely and explain each release"
  default_prompt: "Use $kepler-upgrade to plan a safe in-place Kepler plugin upgrade, preserve my existing Hub and repositories, and show what changed."

policy:
  allow_implicit_invocation: true
