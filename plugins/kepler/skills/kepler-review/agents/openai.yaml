interface:
  display_name: "Kepler Review"
  short_description: "Review changes with ownership context"
  default_prompt: "Use $kepler-review to review this change and lead with actionable findings."
policy:
  allow_implicit_invocation: true
