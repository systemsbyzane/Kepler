interface:
  display_name: "Kepler Automations"
  short_description: "Design safe read-only recurring checks"
  default_prompt: "Use $kepler-automations to design this recurring check with stable finding keys and read-only authority."
