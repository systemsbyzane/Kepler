interface:
  display_name: "Kepler"
  short_description: "Coordinate portable multi-project work"
  default_prompt: "Use $kepler to route this request by exact project path, keep logical and runtime project identities separate, and return without monitoring."
