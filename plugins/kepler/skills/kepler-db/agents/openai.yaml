interface:
  display_name: "Kepler Database"
  short_description: "Guide safe, reliable database work"
  default_prompt: "Use $kepler-db to evaluate this database design or change using durable engineering and operational practices."
policy:
  allow_implicit_invocation: true
